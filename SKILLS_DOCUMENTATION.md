# 🎯 Система Скиллов для DeadCycle - Полная документация

## 📋 Что было сделано

Реализована полная **система скиллов** для китов (ролей) в DeadCycle. Каждый кит может иметь уникальные скиллы, которые активируются при клике на специальный предмет и тратят опыт (Minecraft levels).

---

## 🏗️ Архитектура системы

### 1. **Интерфейс `Skill`** (`Skill.java`)
Базовый интерфейс для всех скиллов. Определяет методы:
- `getId()` - уникальный ID скилла
- `getDisplayName()` - название в UI
- `getXpCost(Player)` - стоимость в опыте
- `getCooldownMs()` - кулдаун в миллисекундах
- `canUse(Player)` - проверка возможности активации
- `activate(Player)` - выполнение скилла
- `reset()` - сброс параметров при перезагрузке

### 2. **Класс `SkillManager`** (`SkillManager.java`)
Менеджер системы скиллов с функционалом:
- **Регистрация скиллов**: `registerSkill(Skill)`
- **Получение скилла по ID**: `getSkill(String skillId)`
- **Получение скилла по киту**: `getSkillForKit(Kit kit)`
- **Управление кулдаунами**: `setCooldown()`, `getRemainingCooldown()`
- **Активация**: `tryActivate(Player, skillId)` - проверяет условия и активирует

### 3. **Скилл Лучника `ArcherRainSkill`** (`ArcherRainSkill.java`)
Пример реализации скилла "Ливень стрел" для кита ARCHER:
- **Эффект**: Спавнятся 10 стрел сверху в радиусе 10 блоков, падают вниз
- **Стоимость**: 5 уровней опыта (по умолчанию)
- **Кулдаун**: 30 секунд
- **Стрелы**: Не наносят урон, не могут быть подобраны

### 4. **Слушатель `SkillListener`** (`SkillListener.java`)
Обработчик события ПКМ (правый клик) по предмету активатора скилла:
- Проверяет тип предмета (скилл-активатор)
- Определяет кит игрока
- Проверяет кулдаун
- Вызывает `SkillManager.tryActivate()`

### 5. **Интеграция в плагин** (`DeadCyclePlugin.java`)
- Создание `SkillManager` в `onEnable()`
- Регистрация `SkillListener`
- Getter метод `skills()`

### 6. **Поддержка в `KitManager`** (`KitManager.java`)
- Выдача предмета активатора скилла лучнику при выборе кита
- Методы проверки: `isSkillActivator(ItemStack)`
- Создание предмета: `makeSkillActivatorItem()`

---

## 🎮 Как использовать

### Для игрока (Лучник):
1. Выбрать кит **ARCHER** (команда `/kit`)
2. Получить 💎 **"⚡ Активатор скилла"** в инвентаре
3. Нажать **ПКМ** (правый клик) с активатором в руке
4. Стрелы начнут падать в радиусе 10 блоков сверху
5. Ждать 30 секунд кулдауна перед следующей активацией

### Стоимость и требования:
- **XP требуется**: 5 уровней (стандартно)
- **Кулдаун**: 30 секунд
- **Радиус**: 10 блоков
- **Стрел**: 10 штук

---

## ⚙️ Конфигурация

### `config.yml` - параметры скилла
```yaml
skills:
  archer:
    rain:
      xp_cost: 5                    # Стоимость в levels
      cooldown_ms: 30000             # Кулдаун в мс (30 сек)
      arrow_count: 10               # Количество стрел
      radius: 10.0                  # Радиус спавна
      spawn_height: 15.0            # Высота падения
```

### `config.yml` - параметры XP для ARCHER
```yaml
kit_xp:
  archer:
    exp_per_zombie: 2              # XP за убийство зомби
    level_xp_base: 65              # Базовый XP для уровня 1
    level_xp_add_per_level: 40     # XP на каждый следующий уровень
    max_level: 10                  # Максимальный уровень
```

---

## 📝 Как добавить новый скилл

### Шаг 1: Создать класс скилла
```java
public class NameSkill implements Skill {
    private final DeadCyclePlugin plugin;
    
    public NameSkill(DeadCyclePlugin plugin) {
        this.plugin = plugin;
        loadConfig();
    }
    
    @Override
    public String getId() { return "skill_id"; }
    
    @Override
    public String getDisplayName() { return "§6Название скилла"; }
    
    @Override
    public int getXpCost(Player p) { return 5; }
    
    @Override
    public long getCooldownMs() { return 30000; }
    
    @Override
    public boolean canUse(Player p) { 
        return p != null && p.getLevel() >= getXpCost(p); 
    }
    
    @Override
    public String getErrorMessage(Player p) { 
        if (p.getLevel() < getXpCost(p))
            return "§cНет опыта"; 
        return null;
    }
    
    @Override
    public void activate(Player p) {
        // Реализация эффекта скилла
        p.sendMessage("§6Скилл активирован!");
    }
    
    @Override
    public void reset() { 
        // Перезагрузка конфига
    }
}
```

### Шаг 2: Зарегистрировать в SkillManager
```java
// В SkillManager.registerSkills()
registerSkill(new NameSkill(plugin));
```

### Шаг 3: Добавить в getSkillForKit()
```java
case MINER -> getSkill("miner_skill_id");  // Пример
```

### Шаг 4: Выдать предмет активатора в KitManager
```java
private void giveMiner(Player p) {
    // ... существующие предметы ...
    p.getInventory().addItem(makeSkillActivatorItem());
}
```

---

## 🔧 Улучшение ARCHER XP системы

Лучник теперь получает **+2 XP за каждого убитого зомби** (как Fighter и Berserk):
- **Уровень 1**: 0-65 XP
- **Уровень 2**: 65-145 XP
- **Уровень 3**: 145-225 XP
- ... и так далее до уровня 10

Параметры можно изменить в `config.yml` секции `kit_xp.archer`.

---

## 📊 Файлы системы

| Файл | Описание |
|------|---------|
| `Skill.java` | Интерфейс скилла |
| `SkillManager.java` | Менеджер скиллов и кулдаунов |
| `ArcherRainSkill.java` | Реализация скилла "Ливень стрел" |
| `SkillListener.java` | Обработчик ПКМ на активаторе |
| `KitManager.java` | Поддержка выдачи активатора |
| `ProgressManager.java` | Система XP с поддержкой ARCHER |
| `EconomyManager.java` | Выдача XP ARCHER за убийства |

---

## 🚀 Примеры расширения

### Скилл Майнера: "Взрыв"
```java
public class MinerBlastSkill implements Skill {
    @Override
    public void activate(Player p) {
        Location loc = p.getLocation();
        loc.getWorld().createExplosion(loc, 3f, false, false);
        p.sendMessage("§6Взрыв!");
    }
}
```

### Скилл Ботбилдера: "Щит"
```java
public class BuilderShieldSkill implements Skill {
    @Override
    public void activate(Player p) {
        p.addPotionEffect(new PotionEffect(
            PotionEffectType.RESISTANCE, 200, 2, true, false, true
        ));
        p.sendMessage("§6Щит активирован!");
    }
}
```

---

## 🎯 Балансировка

### Рекомендуемые параметры:
| Скилл | XP | Кулдаун | Эффект |
|-------|----|---------|----|
| Archer Rain | 5 | 30s | 10 стрел |
| Fighter Berserk | 4 | 20s | Усиление |
| Miner Blast | 6 | 40s | Взрыв |
| Builder Shield | 3 | 25s | Защита |
| Berserk Leap | 4 | 15s | Прыжок |

---

## ✨ Особенности

✅ **Гибкая система**: Легко добавлять новые скиллы
✅ **Кулдауны**: Предотвращают спам
✅ **Конфигурация**: Все параметры в config.yml
✅ **Интеграция**: Работает с существующей XP системой
✅ **Проверки**: Автоматические проверки опыта и условий
✅ **Сообщения**: Понятные сообщения об ошибках

---

Система полностью готова к использованию и расширению! 🚀
